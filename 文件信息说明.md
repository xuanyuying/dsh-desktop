# 文件信息说明

DeepSeek Harness 桌面版（dsh-desktop-desk）完整文件清单与用途说明。

---

## 目录结构总览

```
dsh-desktop-desk/
│
├── src/                          # 应用源代码（Electron 主进程 + 渲染层）
│   ├── main.js                   # 主进程入口：创建窗口、IPC、生命周期、余额轮询
│   ├── preload.js                # 预加载脚本：右下角余额浮层 UI + 与主进程通信
│   └── lib/                      # 纯 Node 业务模块（可脱离 GUI 单独测试）
│       ├── harness.js            # dsh 服务检测 / 启动 / 停止
│       └── balance.js            # DeepSeek 余额 API 查询与 API Key 通用解析
│
├── scripts/                      # 工具脚本（测试 / 构建 / 打包）
│   ├── smoke-test.js             # 冒烟测试：服务检测 + 余额 API
│   ├── test-lib.js               # lib 模块集成测试（12 项断言）
│   ├── test-preload.js           # 浮层渲染逻辑测试（11 项断言）
│   ├── build-dist.js             # 一键打包（国内镜像 / 离线，推荐）
│   ├── prepare-builder-cache.js  # 构建工具缓存准备（幂等）
│   ├── download-builder-tools.js # 从 npmmirror 下载 NSIS 等构建工具
│   ├── download-electron.js      # Electron 二进制下载（镜像加速）
│   ├── extract-electron.js       # Electron 二进制解压到 node_modules
│   ├── gen-ico.js                # 生成标准 BMP 格式 .ico 图标
│   └── patch-builder.js          # patch electron-builder（受限环境适配）
│
├── build/                        # 应用图标
│   ├── icon.png                  # 256×256 源图标
│   └── icon.ico                  # 多尺寸 Windows 图标（安装程序用）
│
├── config.example.json           # API Key 配置模板（复制为 ~/.dsh-desktop/config.json 使用）
├── package.json                  # 项目配置：依赖、脚本、electron-builder 打包配置
├── package-lock.json             # npm 依赖锁定文件
├── .gitignore                    # Git 忽略规则
├── 启动桌面端.bat                 # Windows 一键启动脚本（自动安装依赖）
└── README.md                     # 完整使用说明
```

---

## 各文件详细说明

### 核心源代码（src/）

| 文件 | 类型 | 职责 |
|---|---|---|
| `src/main.js` | Electron 主进程 | 应用生命周期；创建窗口加载 Harness；IPC 通道（app:info / balance:refresh / app:quit）；余额定时轮询（30s）；启动/退出时管理 dsh 服务 |
| `src/preload.js` | 预加载脚本 | 在页面右下角注入余额浮层（💰 余额 + 更新时间），悬停显示明细 tooltip，点击手动刷新；通过 contextBridge 暴露 `window.dshDesktop` API |
| `src/lib/harness.js` | 纯 Node 模块 | 端口探测、HTTP 就绪检测、dsh 入口定位（npx 缓存/PATH）、服务启动（node + bin.js）、退出时 taskkill 进程树 |
| `src/lib/balance.js` | 纯 Node 模块 | 余额 API 调用（https）；API Key 通用解析（环境变量 → ~/.dsh-desktop/config.json → ~/.dsh/.credentials.yaml） |

### 脚本（scripts/）

| 文件 | 用途 |
|---|---|
| `scripts/build-dist.js` | **推荐打包入口**：下载工具 → 准备缓存 → patch → 离线打包 → 输出安装包 |
| `scripts/prepare-builder-cache.js` | 将已下载的 .7z 解压到 electron-builder 期望的 hash 目录并标记缓存完成 |
| `scripts/download-builder-tools.js` | 从 npmmirror 下载 nsis / nsis-resources / winCodeSign 三个构建工具（含 SHA256 校验） |
| `scripts/smoke-test.js` | 快速冒烟：Harness 端口、HTTP 就绪、dsh 入口、API Key、余额 API |
| `scripts/test-lib.js` | lib 模块 12 项断言（含 API Key 优先级测试） |
| `scripts/test-preload.js` | 浮层 UI 11 项断言（mock electron + DOM） |
| `scripts/download-electron.js` | Electron 运行时下载（npmmirror 优先，GitHub 兜底） |
| `scripts/extract-electron.js` | Electron zip 解压 + 写 path.txt（模拟 postinstall） |
| `scripts/gen-ico.js` | 用 System.Drawing 生成多尺寸 BMP 格式 .ico |
| `scripts/patch-builder.js` | 将 node_modules 收集器改为 TRAVERSAL（避免 spawn npm） |

### 配置与资源

| 文件 | 用途 |
|---|---|
| `config.example.json` | API Key 配置模板；复制到 `%USERPROFILE%\.dsh-desktop\config.json` 后填写自己的 Key |
| `package.json` | 声明 `electron` / `electron-builder` 依赖；`start` / `dist` / `verify` 等脚本；`electronDist` 指向本地 Electron 实现离线打包 |
| `build/icon.ico` | 安装程序与窗口图标（16~256 多尺寸） |
| `启动桌面端.bat` | 双击启动：首次自动 `npm install` + 下载/解压 Electron，然后 `npx electron .` |

---

## 构建产物（首次打包后生成，不在源码中）

| 目录/文件 | 说明 |
|---|---|
| `node_modules/` | npm 依赖（Electron 等） |
| `.npm-cache/` | npm 本地缓存 |
| `.builder-cache/` | electron-builder 构建工具缓存（NSIS/winCodeSign/7zip） |
| `dist/DSH Desktop Setup 1.0.0.exe` | **最终安装程序（可执行）** |
| `dist/win-unpacked/DSH Desktop.exe` | 免安装版主程序 |

---

## 隐私说明（通用版）

- ✅ 源码中**不含任何个人 API Key / 用户名 / 路径**
- ✅ API Key 仅在运行时从用户本机配置读取（环境变量 / 用户目录配置文件 / Harness 凭据文件），绝不写入项目源码
- ✅ 所有构建工具均从国内镜像（npmmirror）下载，不访问 GitHub
